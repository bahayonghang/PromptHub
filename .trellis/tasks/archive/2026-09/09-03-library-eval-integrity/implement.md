# 导入安全与列表性能 — 实现清单

1. ZIP 读取用 `take(MAX+1)`。
2. 导入：SQL commit 后再安装媒体。
3. schema v7：FK + 孤儿清理测试。
4. 启动扫描 `running` → `error`。
5. 列表 DTO + `library.counts`（或 `prompt.counts`）命令 + 前端改 load。
6. 可选：FTS UPDATE WHEN 索引列变化。
7. `just test-rust` portable/storage/prompt；`npx vitest run src/features/prompts/promptStore.test.ts`。
